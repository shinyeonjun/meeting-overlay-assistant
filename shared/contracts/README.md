# contracts

`contracts/`는 클라이언트와 서버 사이의 통신 계약을 정리하는 위치다.

여기에는 두 종류만 둔다.

1. `api/`
   - HTTP API 요청/응답 계약
2. `websocket/`
   - 실시간 자막, 이벤트 전송 같은 WebSocket 계약

원칙은 단순하다.

- 코드보다 계약을 먼저 고정한다.
- 계약은 사람이 읽는 문서와 기계가 읽는 JSON Schema를 함께 둔다.
- 클라이언트와 서버는 이 계약을 기준으로 각각 구현한다.
