# Schemas

`shared/schemas`는 더 이상 활성 계약 디렉토리가 아니다.

현재 공통 계약은 아래 위치로 일원화됐다.

- `shared/contracts/api/`
- `shared/contracts/websocket/`
- `shared/enums/`

예전 설명 문서는 단계적으로 정리 중이며, 새 계약을 추가할 때는 이 폴더를 쓰지 않는다.
