{
  "name": "caps-overlay-legacy-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "overlay:dev": "npm --prefix ..\\client\\overlay run overlay:dev",
    "overlay:build": "npm --prefix ..\\client\\overlay run overlay:build",
    "overlay:tauri:clean": "npm --prefix ..\\client\\overlay run overlay:tauri:clean",
    "overlay:tauri:dev": "npm --prefix ..\\client\\overlay run overlay:tauri:dev",
    "overlay:tauri:build": "npm --prefix ..\\client\\overlay run overlay:tauri:build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.1",
    "vite": "^6.2.0"
  }
}
