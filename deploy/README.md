# deploy

`deploy/`는 배포 구현을 담는 자리가 아니라, 앞으로 추가될 설치/운영 자산의 자리다.

현재 단계에서는 실제 배포 스크립트나 서비스 등록 코드를 넣지 않는다.

## 현재 목적

- 서버 설치형 제품 구조를 미리 문서화
- 배포 관련 파일이 들어갈 위치를 고정
- 나중에 CLI/설치 스크립트를 넣을 때 경로를 흔들지 않게 함

## 하위 디렉토리

- `client/`
  - 클라이언트 배포 자산 자리
- `server/`
  - 서버 배포 자산 자리
- `templates/`
  - 설정 템플릿 자리
